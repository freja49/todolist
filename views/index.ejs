<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
</head>

<body>
    <div>
        <img src="/css/bg-desktop-light.jpg">
    </div>
    <div class="body">
        <div class="title">
            <h3> TODO -- <%= day %></h3>
        </div>
        <div class="main">
            <% items.forEach(function(item){ %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%=item.id%>" onChange=this.form.submit() class="checkbox">
                    <p> <%= item.name%></p>
                    <a href="/edit/<%=item._id%>" class="edit">EDIT</a>
                </div>
            </form>
            <%  }) %>


        </div>
        <form action="/" method="post" class="add">
            <input type="test" name="newItem" placeholder="New Item">
            <button type="submit" name="submit">ADD</button>
        </form>
    </div>